NODE_ENV=development
PORT=5000
CORS_ORIGIN=http://localhost:3000

# Required starting with the persistence phase.
MONGODB_URI=

# Required starting with the authentication phase.
JWT_SECRET=

# Required starting with the AI generation phase.
GEMINI_API_KEY=
